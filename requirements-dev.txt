# Development dependencies for MonkaMOO
# Install with: pip install -r requirements-dev.txt

# Linting and formatting
ruff>=0.1.0
black>=23.0.0
isort>=5.12.0
mypy>=1.5.0

# Testing (for future use)
pytest>=7.0.0
pytest-asyncio>=0.21.0
